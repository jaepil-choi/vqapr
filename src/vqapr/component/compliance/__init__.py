"""The Compliance role: its contract (`base.py`), what the framework makes of a finding
(`report.py`), the two shipped rules (`no_short.py`, `single_name_cap.py`) and how a rule is found
by its shipped name (`shipped.py`)."""
